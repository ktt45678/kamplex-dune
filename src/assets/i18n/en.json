{
  "homeFooter": {
    "description": "Online Movie and TV Show streaming site",
    "information": "Information",
    "aboutUs": "About us",
    "project": "Project",
    "hosting": "Hosting",
    "repository": "Repository",
    "support": "Support",
    "faq": "FAQ",
    "privacy": "Privacy",
    "termOfUse": "Term of use",
    "helpCenter": "Help center",
    "contact": "Contact",
    "address": "Address",
    "addressValue": "Vietnam",
    "email": "Email",
    "emailValue": "support@kamplex.tk",
    "phone": "Phone",
    "phoneValue": "0123456789",
    "copyright": "2021 Kam Corp © All Rights not Reserved"
  },
  "homeHeader": {
    "home": "Home",
    "movies": "Movies",
    "tvShows": "TV Shows",
    "signIn": "Sign in",
    "signUp": "Sign up",
    "search": "Search"
  },
  "validationErrors": {
    "general": {
      "username": {
        "required": "Username is required",
        "minlength": "Username must be at least 3 characters",
        "maxlength": "Username must not be longer than 32 characters"
      },
      "email": {
        "required": "Email is required",
        "email": "Email must be valid"
      },
      "password": {
        "required": "Password is required",
        "minlength": "Password must be at least 8 characters",
        "maxlength": "Password must not be longer than 128 characters"
      },
      "birthdate": {
        "required": "Date of birth is required",
        "validShortDate": "Date of birth must be a valid date",
        "shortDateBefore": "Date of birth must be before today"
      }
    },
    "signIn": {
      "email": {
        "required": "{{ validationErrors.general.email.required }}",
        "email": "{{ validationErrors.general.email.email }}"
      },
      "password": {
        "required": "{{ validationErrors.general.password.required }}",
        "maxlength": "{{ validationErrors.general.password.maxlength }}"
      }
    },
    "signUp": {
      "username": {
        "required": "{{ validationErrors.general.username.required }}",
        "maxlength": "{{ validationErrors.general.username.maxlength }}",
        "minlength": "{{ validationErrors.general.username.minlength }}"
      },
      "email": {
        "required": "{{ validationErrors.general.email.required }}",
        "email": "{{ validationErrors.general.email.email }}"
      },
      "password": {
        "required": "{{ validationErrors.general.password.required }}",
        "maxlength": "{{ validationErrors.general.password.maxlength }}",
        "minlength": "{{ validationErrors.general.password.minlength }}"
      },
      "birthdate": {
        "required": "{{ validationErrors.general.birthdate.required }}",
        "validShortDate": "{{ validationErrors.general.birthdate.validShortDate }}",
        "shortDateBefore": "{{ validationErrors.general.birthdate.shortDateBefore }}"
      }
    },
    "createGenre": {
      "name": {
        "required": "Name is required",
        "maxlength": "Name must not be longer than 32 characters"
      }
    },
    "updateGenre": {
      "name": {
        "maxlength": "{{ validationErrors.createGenre.name.maxlength }}"
      }
    },
    "createProducer": {
      "name": {
        "required": "Name is required",
        "maxlength": "Name must not be longer than 150 characters"
      }
    },
    "updateProducer": {
      "name": {
        "required": "{{ validationErrors.createProducer.name.required }}",
        "maxlength": "{{ validationErrors.createProducer.name.maxlength }}"
      }
    },
    "createMedia": {
      "title": {
        "required": "Title is required",
        "maxlength": "Title must not be longer than 500 characters"
      },
      "originalTitle": {
        "maxlength": "Original title must not be longer than 500 characters"
      },
      "overview": {
        "required": "Overview time is required",
        "minlength": "Overview must be at least 10 characters",
        "maxlength": "Overview must not be longer than 2000 characters"
      },
      "runtime": {
        "required": "Running time is required",
        "max": "Running time must not be greater than 10000",
        "min": "Running time must not be a negative number"
      },
      "releaseDate": {
        "required": "Release date is required",
        "validShortDate": "Release date must be a valid date"
      },
      "lastAirDate": {
        "validShortDate": "Last air date must be a valid date"
      }
    },
    "updateMedia": {
      "title": {
        "required": "{{ validationErrors.createMedia.title.required }}",
        "maxlength": "{{ validationErrors.createMedia.title.maxlength }}"
      },
      "originalTitle": {
        "maxlength": "{{ validationErrors.createMedia.originalTitle.maxlength }}"
      },
      "overview": {
        "required": "{{ validationErrors.createMedia.overview.required }}",
        "minlength": "{{ validationErrors.createMedia.overview.minlength }}",
        "maxlength": "{{ validationErrors.createMedia.overview.maxlength }}"
      },
      "runtime": {
        "required": "{{ validationErrors.createMedia.runtime.required }}",
        "max": "{{ validationErrors.createMedia.runtime.max }}",
        "min": "{{ validationErrors.createMedia.runtime.min }}"
      },
      "releaseDate": {
        "required": "{{ validationErrors.createMedia.releaseDate.required }}",
        "validShortDate": "{{ validationErrors.createMedia.releaseDate.validShortDate }}"
      },
      "lastAirDate": {
        "validShortDate": "{{ validationErrors.createMedia.lastAirDate.validShortDate }}"
      }
    },
    "addMediaVideo": {
      "name": {
        "maxlength": "Name must not be longer than 50 characters"
      },
      "url": {
        "required": "URL is required",
        "maxlength": "URL must not be longer than 1000 characters"
      }
    },
    "updateMediaVideo": {
      "name": {
        "maxlength": "{{ validationErrors.addMediaVideo.name.maxlength }}"
      },
      "url": {
        "required": "{{ validationErrors.addMediaVideo.url.required }}",
        "maxlength": "{{ validationErrors.addMediaVideo.url.maxlength }}"
      }
    },
    "addMediaSubtitle": {
      "language": {
        "required": "Subtitle language is required"
      },
      "file": {
        "required": "File is required",
        "maxFileSize": "Maximum file size is 500KB",
        "fileExtension": "Invalid subtitle file"
      }
    },
    "addMediaSource": {
      "file": {
        "required": "File is required",
        "maxFileSize": "Maximum file size is 40GB",
        "fileExtension": "File must be in .mp4, .mkv, .webm or .m2ts format"
      }
    },
    "createEpisode": {
      "episodeNumber": {
        "required": "Episode number is required",
        "min": "Episode number must not be a negative number",
        "max": "Episode number must not be greater than 10000"
      },
      "name": {
        "required": "Name is required",
        "maxlength": "Name must not be longer than 500 characters"
      },
      "overview": {
        "minlength": "Overview must be at least 10 characters",
        "maxlength": "Overview must not be longer than 2000 characters"
      },
      "runtime": {
        "required": "Running time is required",
        "min": "Running time must not be a negative number",
        "max": "Running time must not be greater than 10000"
      },
      "airDate": {
        "required": "Air date is required",
        "validShortDate": "Air date must be a valid date"
      }
    },
    "updateEpisode": {
      "episodeNumber": {
        "required": "{{ validationErrors.createEpisode.episodeNumber.required }}",
        "min": "{{ validationErrors.createEpisode.episodeNumber.min }}",
        "max": "{{ validationErrors.createEpisode.episodeNumber.max }}"
      },
      "name": {
        "required": "{{ validationErrors.createEpisode.name.required }}",
        "maxlength": "{{ validationErrors.createEpisode.name.maxlength }}"
      },
      "overview": {
        "minlength": "{{ validationErrors.createEpisode.overview.minlength }}",
        "maxlength": "{{ validationErrors.createEpisode.overview.maxlength }}"
      },
      "runtime": {
        "required": "{{ validationErrors.createEpisode.runtime.required }}",
        "min": "{{ validationErrors.createEpisode.runtime.min }}",
        "max": "{{ validationErrors.createEpisode.runtime.max }}"
      },
      "airDate": {
        "required": "{{ validationErrors.createEpisode.airDate.required }}",
        "validShortDate": "{{ validationErrors.createEpisode.airDate.validShortDate }}"
      }
    }
  },
  "errorMessagesHeader": "An error occurred",
  "errorMessages": {
    "uploadPosterTooLarge": "Your file is too large, recommended size is 8MB or less",
    "uploadBackdropTooLarge": "{{ errorMessages.uploadPosterTooLarge }}",
    "uploadStillTooLarge": "{{ errorMessages.uploadPosterTooLarge }}",
    "uploadSubtitleTooLarge": "Your file is too large, recommended size is 500KB or less"
  },
  "window": {
    "leaveSite": "Leave site? Changes you made may not be saved."
  }
}
