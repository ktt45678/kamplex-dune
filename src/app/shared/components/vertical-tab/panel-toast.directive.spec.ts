import { PanelToastDirective } from './panel-toast.directive';

describe('PanelToastDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelToastDirective();
    expect(directive).toBeTruthy();
  });
});
