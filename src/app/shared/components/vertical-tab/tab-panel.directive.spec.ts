import { TabPanelDirective } from './tab-panel.directive';

describe('TabPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new TabPanelDirective();
    expect(directive).toBeTruthy();
  });
});
