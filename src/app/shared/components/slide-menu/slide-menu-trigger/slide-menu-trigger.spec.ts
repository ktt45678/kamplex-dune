import { SlideMenuTriggerDirective } from './slide-menu-trigger';

describe('SlideMenuTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideMenuTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
