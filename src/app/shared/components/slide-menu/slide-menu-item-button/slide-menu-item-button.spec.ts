import { SlideMenuItemButton } from './slide-menu-item-button';

describe('SlideMenuItemCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideMenuItemButton();
    expect(directive).toBeTruthy();
  });
});
