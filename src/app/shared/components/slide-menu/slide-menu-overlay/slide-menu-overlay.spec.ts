import { SlideMenuOverlay } from './slide-menu-overlay';

describe('SlideMenuOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideMenuOverlay();
    expect(directive).toBeTruthy();
  });
});
