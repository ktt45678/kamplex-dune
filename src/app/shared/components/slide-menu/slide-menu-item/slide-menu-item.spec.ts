import { SlideMenuItem } from './slide-menu-item';

describe('SlideMenuItemDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideMenuItem();
    expect(directive).toBeTruthy();
  });
});
