import { SlideMenuAimDirective } from './slide-menu-aim.directive';

describe('SlideMenuAimDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideMenuAimDirective();
    expect(directive).toBeTruthy();
  });
});
