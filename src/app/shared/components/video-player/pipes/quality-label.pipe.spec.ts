import { QualityLabelPipe } from './quality-label.pipe';

describe('QualityLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new QualityLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
