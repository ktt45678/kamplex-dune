import { FocusTargetDirective } from './focus-target.directive';

describe('FocusTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusTargetDirective();
    expect(directive).toBeTruthy();
  });
});
