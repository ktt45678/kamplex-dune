import { TextResizeDirective } from './text-resize.directive';

describe('TextResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TextResizeDirective();
    expect(directive).toBeTruthy();
  });
});
