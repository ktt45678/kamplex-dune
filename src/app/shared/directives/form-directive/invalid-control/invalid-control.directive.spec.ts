import { InvalidControlDirective } from './invalid-control.directive';

describe('InvalidControlDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidControlDirective();
    expect(directive).toBeTruthy();
  });
});
