import { AppOverlayOrigin } from './overlay-panel.directive';

describe('OverlayPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new AppOverlayOrigin();
    expect(directive).toBeTruthy();
  });
});
