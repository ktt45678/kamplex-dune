import { ThumbhashUrlPipe } from './thumbhash-url.pipe';

describe('ThumbhashUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ThumbhashUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
