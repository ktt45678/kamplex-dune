import { CharColorPipe } from './char-color.pipe';

describe('CharColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CharColorPipe();
    expect(pipe).toBeTruthy();
  });
});
