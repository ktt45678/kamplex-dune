import { QueryListFindPipe } from './query-list-find.pipe';

describe('QueryListFindPipe', () => {
  it('create an instance', () => {
    const pipe = new QueryListFindPipe();
    expect(pipe).toBeTruthy();
  });
});
