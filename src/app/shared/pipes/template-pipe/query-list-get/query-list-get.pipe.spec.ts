import { QueryListGetPipe } from './query-list-get.pipe';

describe('QueryListGetPipe', () => {
  it('create an instance', () => {
    const pipe = new QueryListGetPipe();
    expect(pipe).toBeTruthy();
  });
});
